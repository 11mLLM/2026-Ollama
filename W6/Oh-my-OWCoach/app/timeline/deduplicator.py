from __future__ import annotations


def deduplicate_events(event_candidates: list[dict], window_sec: float = 3.0) -> list[dict]:
    """Deduplicate repeated event candidates. Placeholder keeps input order."""
    return event_candidates

